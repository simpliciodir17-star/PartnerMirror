name: PartnerMirror
options:
  bundleIdPrefix: com.simpliciodir17
targets:
  PartnerMirror:
    type: application
    platform: iOS
    deploymentTarget: "13.0"
    sources:
      - path: Sources
    info:
      path: Info.plist
    settings:
      PRODUCT_BUNDLE_IDENTIFIER: com.simpliciodir17.PartnerMirror
      SWIFT_VERSION: 5.0
      INFOPLIST_FILE: Info.plist
      ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
    scheme:
      testTargets: []
